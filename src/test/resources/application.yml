spring:
  config:
    activate:
      on-profile: test
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres
    username: postgres.vazqpaobojyddehsxbam
    password: rlaqhtjd8934
logging:
  level:
    org:
      hibernate:
        SQL: debug
        orm:
          jdbc:
            bind: trace